included:
  - Sources
  - Tests
excluded:
  - DerivedData
  - .build
  - Moodments.xcodeproj

line_length:
  warning: 120
  error: 200

identifier_name:
  min_length: 1          # 允许 id / x / y 等短名

type_name:
  min_length: 2

disabled_rules:
  - todo                 # 计划性 TODO 允许存在

reporter: "xcode"
